# Prerequisites
- .NET 9 SDK
- Android SDK + emulator (Pixel 5 or similar)
- VS Code (or CLI)

# Clone repo
- `git clone <repo-url>`
- `cd BoostOrderApp`

# Run on Android emulator
`dotnet build -t:Run -f net9.0-android`
